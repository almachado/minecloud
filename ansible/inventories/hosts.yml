all:
  hosts:
    minecloud:
      ansible_connection: community.aws.aws_ssm
      ansible_aws_ssm_bucket_name: "minecloud-tfstate"
      ansible_aws_ssm_region: "us-east-1"
      ansible_host: "{{ lookup('env', 'MINECLOUD_INSTANCE_ID') }}"